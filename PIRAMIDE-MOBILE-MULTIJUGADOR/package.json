{
  "name": "piramide-mobile-multijugador",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --project vacila-cb462 --only database \"node --test tests/rules.test.mjs\"",
    "test": "npm run test:rules",
    "emulators": "firebase emulators:start --project vacila-cb462 --only auth,database,hosting",
    "deploy": "firebase deploy --project vacila-cb462 --only database"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.15.0",
    "firebase-tools": "^15.21.0"
  }
}
