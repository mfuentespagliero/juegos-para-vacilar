{
  "name": "piramide-multijugador-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "firebase emulators:start --project vacila-cb462 --only auth,database,functions,hosting",
    "deploy": "firebase deploy --project vacila-cb462 --only functions,database"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  }
}
